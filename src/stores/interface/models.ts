export interface MoviesData {
  id: string;
  director: string;
  title: string;
  content: string;
  category: string;
  images: string;
  isNew: boolean;
}

export interface Meta {
  totalCount: number;
}
